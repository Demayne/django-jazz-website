from django.apps import AppConfig

class UserAuthConfig(AppConfig):
    """
    Configuration for the 'user_auth' app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'user_auth'
